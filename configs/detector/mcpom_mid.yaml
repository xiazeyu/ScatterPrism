# MC-POM detector — medium:
#   momentum smearing (σ = 0.5) + cos(θ) acceptance cut at 0.8.

defaults:
  - compose

detectors:
  - _target_: scatterprism.detectors.MomentumSmearing
    sigma: 0.5
  - _target_: scatterprism.detectors.CosThetaCut
    threshold: 0.8
